# sdservice - proxies for sd services, like tusiart, getimg, randomseed, etc

# setup

```shell script
python3 -m virtualenv venv  # virtualenv -p python3 venv
source venv/bin/activate
pip install -r requirements.txt
```

# test

```shell
python tusiart/tusiart.py
```
